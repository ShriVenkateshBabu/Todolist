import styled from "styled-components";

const Button = styled.button`
    letter-spacing: 0.05rem; // Add some letter spacing for better readability
    font-size: 50%;
    background-color: #456B75 ;
    color: white;
    cursor: pointer;
    border: none;
    letters-spacing: 0.05rem;
`

export const TodoButton = styled(Button)``
